# Palette's Journal

## 2025-05-18 - [Accessibility Improvements]
**Learning:** Adding visible focus states (`focus-visible`) improves accessibility without affecting mouse users, making the app more inclusive for keyboard navigation.
**Action:** Consistently apply `focus-visible` styles to all interactive elements (inputs, buttons, links) across the application.
